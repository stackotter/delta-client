name: xcodebuild

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: mxcl/xcodebuild@v1
        with:
          xcode: 12
          platform: macOS
          action: build
          configuration: release
          code-sign-identity: '-'
